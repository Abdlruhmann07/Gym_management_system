<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Members List</title>
    <style>
        .member {
            padding: 10px;
            margin: 5px;
            border: 1px solid #ccc;
        }
    </style>
</head>
<body>
    <h1>Members List</h1>
    <div id="membersList">
        <% paginatedMemberAttendances.forEach(ele => { %>
            <div class="member">
                <h1><%= member.username %></h3>
                <h3><%= ele.memberId %></h3>
                <h3><%= ele.classId %></h3>
                <p><%= ele.checkInTime %></p>
            </div>
        <% }); %>
        </div>
</body>
</html>
